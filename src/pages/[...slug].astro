---
import CmsPage from "../components/CmsPage.astro";
import { staticPages } from "../data/sitemap";

export function getStaticPaths() {
  return staticPages.map((page) => ({
    params: { slug: page.slug },
    props: { page },
  }));
}

const { page } = Astro.props;
---

<CmsPage page={page} />
